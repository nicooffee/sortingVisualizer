Compile requirements:
    - Pthread library
    - Ncurses library

There's a comp.sh file that you can use to compile and execute this program:

    $ bash comp.sh

You can change the value of ALGORITMO macro in sorting.c in order to change the algorithm.
